services:
  pixzap:
    build: .
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - pixzap-data:/app/data
      - ./config.yaml:/app/config.yaml:ro
    restart: unless-stopped

volumes:
  pixzap-data:
